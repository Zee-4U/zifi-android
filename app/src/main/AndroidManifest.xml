<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="ZiFi"
        android:theme="@style/Theme.ZiFi"
        android:usesCleartextTraffic="true">
        <!-- cleartext allowed: router admin panels are plain http on your LAN (192.168.x.x) -->

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.SetupActivity" android:exported="false" />
        <activity android:name=".ui.RouterWebViewActivity" android:exported="false" />
        <activity android:name=".ui.QrShareActivity" android:exported="false" />

    </application>
</manifest>
